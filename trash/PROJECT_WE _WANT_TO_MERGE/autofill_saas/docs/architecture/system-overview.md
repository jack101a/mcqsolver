# System Overview

This repository implements a layered autofill SaaS platform:

- Browser execution layer: Chrome and Firefox extensions
- Rule-first autofill + AI fallback layer
- Workflow orchestration and run tracking
- SaaS backend modules for auth, subscriptions, sync, and admin
- Shared schemas and SDK packages for contract consistency

Primary API service lives in `apps/api`.
