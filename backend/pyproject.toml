[tool.ruff]
line-length = 100
target-version = "py311"
exclude = [
  "migrations",
  "tessdata",
  "logs",
]

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]
ignore = ["E501", "B008", "B904", "E712", "E741"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
